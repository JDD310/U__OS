# OSINT Monitor — Environment Variables
# Copy to .env and fill in all values before deploying.
#
# Generate strong passwords:  openssl rand -base64 32

# ── Database ─────────────────────────────────────────────────────────────────
DB_PASSWORD=changeme

# ── Redis ────────────────────────────────────────────────────────────────────
REDIS_PASSWORD=changeme

# ── Telegram (from https://my.telegram.org → API development tools) ──────────
TELEGRAM_API_ID=
TELEGRAM_API_HASH=

# ── Domain ───────────────────────────────────────────────────────────────────
# The domain Caddy will serve and obtain TLS certificates for.
# Set to localhost for local-only access (Caddy will use a self-signed cert).
DOMAIN=localhost
